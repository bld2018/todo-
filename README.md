# 🎯 芝麻分精准匹配系统

一个基于 Supabase 的芝麻分组队匹配系统，支持精准匹配总分恰好等于 2026 的组合。

## ✨ 功能特性

- ✅ **精准匹配**：查找总分恰好等于 2026 的组合
- ✅ **多组合显示**：显示所有符合条件的组合及成员
- ✅ **安全删除**：删除需备注原因，操作记录不可删除
- ✅ **管理员后台**：数据管理、审计日志查看
- ✅ **云端存储**：数据持久化，多设备同步
- ✅ **响应式设计**：完美适配手机、平板、电脑

## 🚀 部署步骤

### 第一步：创建 Supabase 项目

1. 访问 [Supabase](https://supabase.com) 并注册账号
2. 点击 `New Project` 创建新项目
3. 填写项目信息：
   - 项目名称：`sesame-score-matcher`
   - 密码：设置一个强密码（记住它！）
   - 区域：选择离你最近的（如 Singapore）
4. 点击 `Create new project`，等待 2-3 分钟

### 第二步：创建数据库表

1. 进入项目 → 左侧菜单 `Table Editor` → `New Table`
2. 创建 `participants` 表：

| 列名 | 类型 | 主键 | 默认值 |
|------|------|------|--------|
| id | text | ✅ | - |
| name | text | ❌ | - |
| score | int4 | ❌ | - |
| created_at | timestamptz | ❌ | now() |

3. 创建 `audit_log` 表：

| 列名 | 类型 | 主键 | 默认值 |
|------|------|------|--------|
| id | uuid | ✅ | gen_random_uuid() |
| participant_id | text | ❌ | - |
| participant_name | text | ❌ | - |
| participant_score | int4 | ❌ | - |
| delete_reason | text | ❌ | - |
| deleted_at | timestamptz | ❌ | now() |

### 第三步：获取 API 配置

1. 左侧菜单 `Project Settings` → `API`
2. 复制以下信息：
   - Project URL
   - `anon` public key
3. 打开 `config.js`，替换以下内容：

```javascript
const SUPABASE_URL = 'https://your-project.supabase.co'; // 替换为你的 Project URL
const SUPABASE_ANON_KEY = 'your-anon-key-here'; // 替换为你的 anon key
